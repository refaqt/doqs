{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/refaqt/doqs/schemas/build.schema.json",
  "title": "DOQS build lockfile",
  "type": "object",
  "required": [
    "schema",
    "machine"
  ],
  "properties": {
    "schema": {
      "const": "doqs-build-v1"
    },
    "machine": {
      "type": "string"
    },
    "built-date": {
      "type": "string",
      "format": "date"
    },
    "owner": {
      "type": "string"
    },
    "location": {
      "type": "string"
    },
    "base": {
      "type": "object",
      "properties": {
        "repo": {
          "type": "string",
          "format": "uri",
          "description": "Where to fetch this from. Without it nobody knows."
        },
        "version": {
          "type": "string"
        },
        "commit": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$",
          "description": "The exact commit. A tag can be moved or deleted; this cannot, so it is what makes the record openable."
        }
      },
      "required": [
        "repo",
        "version",
        "commit"
      ]
    },
    "module": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "path",
          "version",
          "repo",
          "commit"
        ],
        "properties": {
          "path": {
            "type": "string"
          },
          "version": {
            "type": "string"
          },
          "model": {
            "type": "string",
            "description": "Parametric model slug declared by the composition's core module."
          },
          "adapter": {
            "type": "string"
          },
          "composition": {
            "type": "string",
            "description": "Path of the composition module inside a family repo, when `path` points at a family."
          },
          "options": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Option module paths inside the family, when they are not implied by the composition."
          },
          "sku": {
            "type": "string",
            "description": "Commercial catalogue id from the family's catalog.toml."
          },
          "repo": {
            "type": "string",
            "format": "uri",
            "description": "Where to fetch this from. Without it nobody knows."
          },
          "commit": {
            "type": "string",
            "pattern": "^[0-9a-f]{40}$",
            "description": "The exact commit. A tag can be moved or deleted; this cannot, so it is what makes the record openable."
          }
        }
      }
    },
    "modification": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "date",
          "description"
        ],
        "properties": {
          "date": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "bom-impact": {
            "type": "string"
          }
        }
      }
    }
  },
  "description": "A record of one physical machine. It is only worth having if you can still get back what went into it, so every entry names the repository, the readable tag and the exact commit. See docs/decisions/2026-09-18_build-records.md."
}
