# Auto detect text files and perform LF normalization
* text=auto

# Consumer-root helpers (templates copied to machine roots)
setup-tooling.sh text eol=lf
templates/setup-tooling/setup-tooling.sh text eol=lf
templates/syson/syson.sh text eol=lf
