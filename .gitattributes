# Auto detect text files and perform LF normalization
* text=auto

# Shell files must never be stored with CRLF. A hook or helper that arrives
# with CRLF fails with "bad interpreter", and install_root_tools.py would copy
# those bytes on into every machine repository.
*.sh text eol=lf
